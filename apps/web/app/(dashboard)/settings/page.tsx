import { EmptyState } from "@/components/empty-state";

export default function SettingsPage() {
  return <EmptyState title="Settings are not configured" description="Organization, preferences, and integrations will be managed here." />;
}
